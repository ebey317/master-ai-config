# M0 Handoff — 2026-05-12 evening

**For all three agents (Claude, Codex, third validator). Single source of truth: `~/.claude/projects/-home-elijah/memory/project_master_ai_state.md` ("WHERE WE LEFT OFF 2026-05-12 evening" section). This file is the focused handoff anchor.**

Plan file: `~/.claude/plans/to-much-cpu-whatever-tranquil-catmull.md`

## State

- HEAD: `fc6ced2` "Add M0 extension chat bridge" (Codex committed via surgical-extract, 18 files +1777/-60). Predecessor: `822b69c`.
- 24 files still dirty on working tree (20 pre-existing rework + 4 unrelated new untracked). NOT part of the bridge slice. Per `feedback_dual_agent_surgical_extract.md`, any future commit using these uses the cp/checkout-HEAD/reapply dance.
- Memory pressure: 6.9 GiB available (was 2.2 GiB). LLaVA on-demand; only `master-ai:latest` resident.
- `~/.master_ai_extension_token` exists (chmod 600, 32-byte hex).
- `master-ai-ui.service` active (running).
- 4/4 auth smoke tests green.
- Also updated by Codex: `~/.master_ai_where_were_we`, `~/.master_ai_memory`, `PROJECTS.md` Pupil board.

## What landed (committed in fc6ced2)

| File | Change | Agent |
|---|---|---|
| `prewarm_master_ai.py` | llava preload entry removed | Claude |
| `master_ai.py:3637, 3848` | conditional `keep_alive` for vision vs master | Claude |
| `typed_actions.py:44-57, 78-79` | `Kind.BROWSER_*` + `DIRECTIVE_KINDS` extended | Claude |
| `typed_actions.py:105-108` | `_DIRECTIVE_LINE_RE` extended for BROWSER_* | Codex |
| `stt_server.py:_cors` (~1040) | origin-echo + `X-Master-AI-Token` allow-header | Claude |
| `stt_server.py:_extension_token / _origin_is_extension / _require_extension_auth` | auth helpers | Claude |
| `stt_server.py:625` `POST /extension/action_result` | Branch B audit-only sink | Claude |
| `stt_server.py:219` `api_handle(payload)` | Non-interactive `/chat` bridge into `master_ai.handle()` | Codex |
| `stt_server.py:53, 76, 95, 116, 138, 201` | `_api_*` helpers (session key, prompt formatter, action parser, blocked-mirror, fallback, page-context formatter) | Codex |
| `stt_server.py:1043-1046` | `/chat` endpoint now calls `api_handle(payload)` | Codex |
| `~/scripts/m0_prereq_check.sh` | preflight script (7 checks + token bootstrap) | Claude |
| `~/scripts/sensei_extension/` (11 files) | M0b extension scaffold (manifest, service_worker, side_panel.*, content_script, options.*, README) | Codex |

## Locked architecture

- **Branch B chosen** — `api_handle` never calls `confirm_run()` (TUI-only, blocks on stdin). Actions returned in `actions[]`. Extension renders confirm UI in side panel. Verdict + result POSTed to `/extension/action_result`. Backend never reaches DOM directly.
- **CORS** — chrome-extension origin echo + same-host echo + reject others. Token required for chrome-extension; same-origin Pupil keeps legacy trust.
- **Pupil retires (UI only)** in M3. `master-ai-ui.service` stays — it IS the backend.
- **Voice in** — local whisper via `POST /stt` from M0b day one. Browser STT is fallback toggle, not default.

## Next gate (M0 verification)

1. `bash ~/scripts/m0_prereq_check.sh` — all green.
2. Sideload `~/scripts/sensei_extension/` via `chrome://extensions` (Developer mode → Load unpacked).
3. Paste `~/.master_ai_extension_token` into extension options.
4. Open side panel, send a prompt, verify reply via `api_handle`.
5. Test BROWSER_* roundtrip: prompt that should propose a click → `actions[]` returned → confirm UI in side panel → `/extension/action_result` records outcome.
6. GREEN through #5 → M0 complete. Begin M1 (full DOM action surface).

## Pending (M1+)

- M1 content script: real selector resolution + mode-gated preview UI + always-confirm category (password/payment/2FA/destructive/ambiguous-submit).
- M2 `chrome.alarms` + setup wizard for "Continue running background apps when Google Chrome is closed" + drift detector.
- M3 Pupil UI archive to `~/scripts/archive/pupil/`. Service stays.
- M5 OmniParser cross-app desktop escalation (deferred, 1-2 weeks scope).

## Open questions

- M7 MCP host adapter — defer to after M5 (locked default) OR pull forward if a concrete MCP server need surfaces?
- M8 Linux desktop shell — defer indefinitely (locked default) OR scope a minimal Electron wrap?
- When to commit the surgical-extract pile?
